# Progress Log

## Learnings

## Decisions
- Packaging: pyproject.toml with src/ layout (no requirements.txt or setup.cfg)
- DB: SQLite via stdlib sqlite3 for MVP (no SQLAlchemy)
- Config: pydantic-settings for env var loading with typed defaults
- Logging: stdlib logging only, no external deps
- Layout: new code under src/smaps/; legacy src/*.py files left untouched
- Phase 0 complete (US-0001..US-0005). US-0006 deferred.
- Chose lightweight code-driven schema versioning (schema_migrations table) instead of Alembic for MVP.
- US-0006 complete. Full Phase 0 done.

## Integration Notes
- `pip install -e .` installs the smaps package in editable mode
- `python -m pytest -q` runs all tests from repo root
- `python -c "import smaps; print('ok')"` verifies package importability
- SQLite DB created on-demand at path from SMAPS_DB_PATH (default: data/smaps.sqlite)
- Single source of truth verification: scripts/verify_scaffold.sh + CI scaffold_verify.yml

## Next Smallest Task
- US-0101: Define canonical data model for OHLCV daily bars (Phase 1 begins).
